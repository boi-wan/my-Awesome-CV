\ProvidesPackage{awesome-cv-class-overrides}

% Fonts are now installed as system fonts in the Docker container (see Makefile).
% No font path overrides needed — awesome-cv.cls will find them automatically.
